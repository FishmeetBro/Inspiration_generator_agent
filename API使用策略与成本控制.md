# API 使用策略与成本控制

## 1. 当前 `.env` 检测结果

已检测到以下条目：

- `OPENAI_API_KEY`：已配置
- `SERPER_API_KEY`：已配置
- `OPENAI_MODEL_NAME=gpt-4o`
- `GROQ_API_KEY`：已配置
- `DEEPSEEK_API_KEY`：已配置
- `CREWAI_TRACING_ENABLED=true`

注意：

- 你当前环境中是 `SERPER_API_KEY`，不是 `SERPAPI_API_KEY`
- 如果后续你真的要接 `SerpAPI` 而不是 `Serper`，需要单独补一个新的 key

## 2. 各 API 在“爆款产品灵感智能体”中的可用场景

### OpenAI

适合：

- 中枢 Agent 终审
- 高质量汇总与淘汰理由生成
- 跨 Agent 结果合并
- 专利/合规结论的最终文字化输出

不适合：

- 大批量低价值初筛
- 长列表候选的反复改写
- 搜索和抓网页

建议定位：

- 作为“最后一跳”的高质量模型
- 不要让所有子 Agent 都默认走 OpenAI

### Serper

适合：

- 爆款关键词搜索
- 新闻和趋势检索
- 跨平台网页快速摸底
- 专利公开网页的快速入口级搜索

不适合：

- 生成文本
- 做复杂判断
- 替代正式专利数据库

建议定位：

- 只负责“找资料”
- 找到结果后交给低成本模型做摘要和结构化

### Groq

适合：

- 场景拆解
- 差评归类
- 批量标签抽取
- 快速重写为结构化输出

不适合：

- 需要最高稳定性的最终商业结论
- 高风险专利判断的最终拍板

建议定位：

- 作为“低成本、超高速”的中间处理层
- 适合多条候选同时跑

### DeepSeek

适合：

- 特征抽取
- 特征迁移
- 候选产品定义生成
- 大批量结构化 JSON 输出

不适合：

- 直接替代搜索
- 直接替代正式专利检索库

建议定位：

- 作为“批量创意生成”和“结构化加工”的主力

## 3. 各 API 的限制与成本控制思路

### OpenAI 成本控制

- 不把 OpenAI 放在每个 Agent 前面
- 只在主 Agent 最终整合、终审、解释理由时调用
- 约束 `max_tokens`
- 优先使用更便宜的 mini 模型做终审草稿，再决定是否升级
- 把已知规则、模板和结构尽量硬编码，减少模型重复输出

### Serper 成本控制

- 先做关键词聚合，再发 1 次搜索，不要每个候选都单独搜
- 优先搜索“类目 + 痛点 + 平台”组合，而不是宽泛自然语言
- 对同一查询做本地缓存
- 初筛时只取前 5 到 10 条结果

### Groq 成本控制

- 用于批量中间层任务，不做最后终审
- 限制输出为短 JSON 或短列表
- 对场景拆解和标签分类任务，保持 prompt 模板固定

### DeepSeek 成本控制

- 优先用于批量生成和结构化重写
- 把长上下文拆成“先提特征，再合并候选”两步
- 输出字段固定，避免开放式长文

## 4. 按当前多 Agent 工作流的优先 API 建议

### 工作流：中枢 Agent 调度

优先：

- OpenAI `gpt-5.4-mini` 或你后续设定的低成本主控模型

原因：

- 中枢只做路由、合并和终审，不是最大调用量节点
- 这里值得用更稳定的模型

### 工作流：爆款挖掘 Agent

优先：

- `SERPER_API_KEY` 做搜索
- `DEEPSEEK_MODEL` 做结果压缩与爆款拆解

备选：

- Groq 负责差评归类和快速标签化

### 工作流：新领域探测 Agent

优先：

- Serper News / Web 搜索
- DeepSeek 做圈层机会摘要

备选：

- Groq 做快速候选归类

### 工作流：特征迁移 Agent

优先：

- DeepSeek

原因：

- 这个环节往往候选最多，最适合用低成本模型批量跑

### 工作流：场景拆解 Agent

优先：

- Groq

原因：

- 场景拆解更像快速规则判断和短文本结构化
- 速度快、成本低，适合高频调用

### 工作流：专利预警 Agent

优先：

- Serper Patents / Web
- OpenAI 做最终风险摘要

备选：

- DeepSeek 先做初筛，人工再复核高风险项

原因：

- 这里不适合全程走最低价模型直接拍板
- 适合“先便宜筛一轮，再高质量总结”

## 5. 贴合 10 美元额度的推荐策略

推荐分层：

- 70% 预算给 DeepSeek + Groq，承担高频批量任务
- 20% 预算给 OpenAI，只做中枢终审和高价值结论
- 10% 预算给 Serper，用于搜索和专利入口检索

具体建议：

- 每轮先让 Serper 拉 1 次搜索结果
- 用 DeepSeek 生成 3 到 5 个候选方案
- 用 Groq 做场景过滤和标签整理
- 只有剩下的 1 到 2 个候选才交给 OpenAI 总结

## 6. 当前最应该调整的一点

你现有 `.env` 里 `OPENAI_MODEL_NAME=gpt-4o`，如果目标真的是把总预算压在 10 美元附近，不建议让它继续作为默认主力模型。

更稳妥的方式是：

- OpenAI 只保留给中枢终审
- 日常大批量节点优先用 DeepSeek 和 Groq
- 把 `.env` 的默认主控模型单独拆成 `OPENAI_LOW_COST_MODEL`
